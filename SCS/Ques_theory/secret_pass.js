(function () {
    const password = "secret password";
    console.log(password);

})(); // This is an IIFE (Immediately Invoked Function Expression) that encapsulates the password variable
// The password variable is not accessible outside this function scope, providing a level of encapsulation.
console.log(password);